#include "main.h"
/**
 * print_sign - main function
 *
 * @n: checks the sign of the number
 *
 * Return: 1 if postitive, 0 if zero, -1 if negative
 *
 */

int print_sign(int n)
{
	if (n > 0)
	{
		return (1):
	}
	else if (n == 0)
	{
		return (0);
	}
	else
	{
		return (-1);
	}
}
