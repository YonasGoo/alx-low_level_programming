#include <stdio.h>

/**
 * main - Entry point
 * @argc: the argument count
 * @argv: the argument vector
 *
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argv;

	if (argc > 1)
	{
	printf("%d\n", argc);
	}
	return (0);
}
