#include <stdlib.h>
#include <stdio.h>
/**
 * main: the main function
 * @argc: the argument count
 * @argv: the argument vector
 * return: always 0
 *
 */
int main(int argc, char *argv[])
{
	if (argc > 1)
	{
	printf("%d\n", argc);
	}
	return (0);
}
